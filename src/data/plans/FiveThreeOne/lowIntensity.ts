import { seconds } from "./seconds";
import { Set } from "./Set";

// export type Cycle = Set[];
export const lowIntensity: Set[] = [
  { multiplier: 65, volume: 5, duration: 60 * seconds },
  { multiplier: 75, volume: 5, duration: 60 * seconds },
  { multiplier: 85, volume: 5, duration: 60 * seconds },
];
